<?xml version="1.0" encoding="UTF-8"?>
<server xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="../../../../schema/schema.xsd">

	<!-- The microProfile-4.0 feature brings in mpOpenTracing-2.0 which expects 
		an OpentracingTracerFactory such as Jaeger or Zipkin, but by default we don't 
		configure that so we can disregard this warning. -->
	<logging hideMessage="CWMOT0010W" />

</server>
